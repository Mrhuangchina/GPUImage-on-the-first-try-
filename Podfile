platform :ios, '9.0'
use_frameworks!

targetsArray = ['GPUImage的使用', 'GPUImage其他滤镜', 'GPUImage美颜相机','GPUImage实时画面']

    targetsArray.each do |t|
        target t do
            pod 'GPUImage'
    end
end
